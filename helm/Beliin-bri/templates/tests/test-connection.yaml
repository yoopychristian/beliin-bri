apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Beliin-bri.fullname" . }}-test-connection"
  labels:
    {{- include "Beliin-bri.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Beliin-bri.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
