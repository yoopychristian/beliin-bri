apiVersion: v1
kind: Service
metadata:
  name: {{ include "Beliin-bri.fullname" . }}
  labels:
    {{- include "Beliin-bri.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Beliin-bri.selectorLabels" . | nindent 4 }}
